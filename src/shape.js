import React from 'react';

const SHape = (props) => {
    const { shape, selectShape } = props;

    return () < div classNAme = { shape }
    onClick = {
            (evt) => {
                selectShape(shape);
            }
        } >
        <
        /div>;
);
};
export default shape;